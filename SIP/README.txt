SIP

time: 2016.4.10

discription:
    I write a program to match the IP-address and computer-name
int the same network-segment with me.

structure:
Start.bat ==> searchName.bat Match.bat Fix.exe
searchName.bat ==> VResult.txt(note the process)/
                   VResultPCIP.txt(IP which is using)/
                   VResultPCN.txt(computer name)/
Match.bat ==> MResult.txt(match IP and name, note the process)
Fix.cpp ==> FResult.txt(IP-address result)
